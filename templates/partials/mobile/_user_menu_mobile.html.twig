<div class="collapse modal-menu rounded-4" id="mobileUserMenu" data-navbar-dropdown-target="mobileUserMenu">
    <div class="bg-white rounded-4 shadow">
        <!-- En-tête avec X pour fermer -->
        <div class="d-flex justify-content-between align-items-center p-3 border-bottom">
            <button class="btn text-dark" data-action="navbar-dropdown#closeMobileUserMenu">
                <i class="bi bi-x-lg"> Close </i>
            </button>
        </div>

        <!-- Liste des options utilisateur -->
        <div class="mobile-menu-content">
            <div class="list-group list-group-flush rounded-bottom-5">
                {% if app.user %}
                    <a href="{{ path('app_account') }}" class="list-group-item list-group-item-action border border-bottom-1">
                        <i class="bi bi-speedometer2 me-2"></i>Dashboard
                    </a>
                    <a href="#" class="list-group-item list-group-item-action">
                        <i class="bi bi-box me-2"></i>Orders
                    </a>
                    <a href="{{ path('app_account_addresses') }}" class="list-group-item list-group-item-action border border-bottom-1">
                        <i class="bi bi-geo-alt me-2"></i>Addresses
                    </a>
                    <a href="{{ path('app_account_wishlist') }}" class="list-group-item list-group-item-action border border-bottom-1">
                        <i class="bi bi-heart me-2"></i>Wishlist
                    </a>
                    <a href="{{ path('app_account') }}" class="list-group-item list-group-item-action border border-bottom-1">
                        <i class="bi bi-gear me-2"></i>Settings
                    </a>
                    <a href="{{ path('app_logout') }}" class="list-group-item list-group-item-action text-danger border">
                        <i class="bi bi-box-arrow-right me-2"></i>Sign out
                    </a>
                {% else %}
                    <a href="{{ path('app_login') }}" class="list-group-item list-group-item-action border border-bottom-1">
                        <i class="bi bi-box-arrow-in-right me-2"></i>Sign in
                    </a>
                    <a href="{{ path('app_register') }}" class="list-group-item list-group-item-action">
                        <i class="bi bi-person-plus me-2"></i>Sign up
                    </a>
                {% endif %}
            </div>
        </div>
    </div>
</div>
